<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- nav style -->
  <link rel="stylesheet" href="/styles/nav.css">
  <link rel="stylesheet" href="/styles/book-pages/show.css">
  <title>Book Show Page</title>
</head>
<body>
  <!-- header partial-->
  <%-include('../nav.ejs')%> 
  <main>
    <section class="bookCover">
      <div class="leftCover"></div>
      <div class="spine"></div>
      <div class="rightCover"></div>
    </section>
    <section class="bookData">
      <div class="leftPage">
        <p>AND on the book add page, maybe make this a Search?</p>
      </div>
      <div class="rightPage">
        <form action="/users/<%=user._id%>/books/<%=book._id%>?_method=PUT" method="POST">
          <h2> Title: <input type="text" name="title" value="<%=book.title%>"></h2>
          <h3>Author: <input type="text" name="author" value="<%=book.author%>"></h3>
          <h4>Publication Date: <input type="text" name="PubDate" value="<%=book.PubDate%>"></h4>
          <hr />
          <p class=''> Summary: <textarea type="text" name="summary" cols="50" rows="8"><%=book.summary%></textarea>
          </p>
          <p class=''>Primary Review:
            <textarea name="primaryReview" cols="50" rows="8" ><%=book.primaryReview%></textarea>
          </p>

          <label for="shelf">Shelf</label>
          <select type="text" name="shelf" id="shelf">
            <% user.bookshelves.forEach(shelf=>{ %>
              <option value="<%=shelf%>"<%= book.shelf===shelf ?'selected': null %>> <%=shelf%></option>
            <% }) %>
            </select>
          
          <p>Color<input type="text" name="color" id=""value="<%=book.color%>">
          </p>
        <button type="submit">Update Book</button>
        </form>
      </div>
      <div class="rightStack"></div>
    </section>
  </main>
  
</body>
</html>